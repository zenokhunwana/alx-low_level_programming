#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/**
 * main - print alphabet in lower case
 * Return: 0
 */
int main(void)
{
char j = 'M';
char out = tolower(j);
putchar(out);
putchar('\n');

return (0);
}
